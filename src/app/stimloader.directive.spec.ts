import { StimloaderDirective } from './stimloader.directive';

describe('StimloaderDirective', () => {
  it('should create an instance', () => {
    // const directive = new StimloaderDirective();
    // expect(directive).toBeTruthy();
  });
});
